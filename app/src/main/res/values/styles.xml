<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Styles for TextInputLayout hint colors -->
    <style name="HintText" parent="TextAppearance.MaterialComponents.Subtitle2">
        <item name="android:textColor">@color/text_secondary</item>
    </style>
    
    <!-- Style for SearchView -->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/text_secondary</item>
        <item name="android:editTextColor">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:tint">@color/primary</item>
    </style>

    <!-- Add a new style for the period spinners -->
    <style name="PeriodSpinnerStyle">
        <item name="android:background">@drawable/bg_spinner</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:minWidth">120dp</item>
    </style>
    
    <!-- Style for card header spinner -->
    <style name="CardHeaderSpinnerStyle">
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:popupBackground">@color/surface</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:textColor">@color/text_on_primary</item>
        <item name="android:textSize">18sp</item>
    </style>
    
    <!-- Style for income card header spinner -->
    <style name="IncomeHeaderSpinnerStyle" parent="CardHeaderSpinnerStyle">
        <item name="android:textColor">@color/white</item>
    </style>
    
    <!-- Style for unpaid card header spinner -->
    <style name="UnpaidHeaderSpinnerStyle" parent="CardHeaderSpinnerStyle">
        <item name="android:textColor">@color/black</item>
    </style>
</resources> 